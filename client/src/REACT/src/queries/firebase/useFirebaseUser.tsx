import React, { useState } from 'react';

const useFirebaseUser = () => {
  const [state, setState] = useState({ isPending: true, hasError: false, data: {} });

  return {}
}

export default useFirebaseUser;